@model List<Depenses>

@{
    ViewData["Title"] = "General expenses list";
}

@if (ViewBag.ErrorMessage != null)
{
    <p class="text-danger">@ViewBag.ErrorMessage</p>
}
else
{
    <div class="card">
        <h3 class="card-header">General Expenses</h3>
        <div class="table-responsive text-nowrap">
            <table class="table">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Date</th>
                        <th>Source</th>
                        <th>Summary</th>
                        <th>Customer</th>
                        <th>Amount</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var depense in Model)
                    {
                        <tr>
                            <td>@depense.Id</td>
                            <td>@depense.DateDepense.ToString("yyyy-MM-dd")</td>
                            <td>@depense.Source</td>
                            <td>@depense.Summary</td>
                            <td>@depense.CustomerId</td>
                            <td>@depense.Amount</td>
                            <td>
                                <div class="col-md-12">
                                    @* <button type="button" class="btn btn-danger">Delete</button> *@
                                    <button type="button" class="btn btn-warning">Update</button>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}